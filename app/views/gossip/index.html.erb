<h1>Gossip n°<%=@id%></h1>
<p>Son titre : <%=Gossip.find(@id).title%> </br>
Son contenu : <%=Gossip.find(@id).content%> </br>
Créé le : <%=Gossip.find(@id).created_at.strftime("%d of %B, %Y") %> </br></br>

Son auteur est : <%=Gossip.find(@id).user.first_name%> <%=Gossip.find(@id).user.last_name%> </br></br>

<%= link_to "Modifier le gossip", edit_gossip_path(@id) %><br />
<%= link_to "Supprimer le gossip", gossip_path(@id), method: :delete %>
</p>

